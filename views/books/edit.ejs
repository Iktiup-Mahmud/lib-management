<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/header.ejs') %>

    <div class="container">
        <form action="/books/update/<%= book.id %>" method="POST">
            <h1>Edit Book: <%= book.title %></h1>
            
            <% if (typeof error !== 'undefined') { %>
                <div class="notification notification-error">
                    <%= error %>
                </div>
            <% } %>
            
            <div class="form-group">
                <label for="title">Title:</label>
                <input type="text" id="title" name="title" value="<%= book.title %>" required>
            </div>
            
            <div class="form-group">
                <label for="author">Author:</label>
                <input type="text" id="author" name="author" value="<%= book.author %>" required>
            </div>
            
            <div class="form-group">
                <label for="genre">Genre:</label>
                <select id="genre" name="genre" required>
                    <option value="">Select Genre</option>
                    <option value="Fiction" <%= book.genre === 'Fiction' ? 'selected' : '' %>>Fiction</option>
                    <option value="Non-Fiction" <%= book.genre === 'Non-Fiction' ? 'selected' : '' %>>Non-Fiction</option>
                    <option value="Science Fiction" <%= book.genre === 'Science Fiction' ? 'selected' : '' %>>Science Fiction</option>
                    <option value="Romance" <%= book.genre === 'Romance' ? 'selected' : '' %>>Romance</option>
                    <option value="Mystery" <%= book.genre === 'Mystery' ? 'selected' : '' %>>Mystery</option>
                    <option value="Biography" <%= book.genre === 'Biography' ? 'selected' : '' %>>Biography</option>
                    <option value="History" <%= book.genre === 'History' ? 'selected' : '' %>>History</option>
                    <option value="Science" <%= book.genre === 'Science' ? 'selected' : '' %>>Science</option>
                    <option value="Fantasy" <%= book.genre === 'Fantasy' ? 'selected' : '' %>>Fantasy</option>
                    <option value="Adventure" <%= book.genre === 'Adventure' ? 'selected' : '' %>>Adventure</option>
                    <option value="Political Satire" <%= book.genre === 'Political Satire' ? 'selected' : '' %>>Political Satire</option>
                    <option value="Dystopian" <%= book.genre === 'Dystopian' ? 'selected' : '' %>>Dystopian</option>
                </select>
            </div>
            
            <div class="form-group">
                <label for="publishedDate">Published Date:</label>
                <input type="date" id="publishedDate" name="publishedDate" 
                       value="<%= book.published_date ? book.published_date.toISOString().split('T')[0] : '' %>" required>
            </div>
            
            <div class="form-group">
                <label for="isbn">ISBN:</label>
                <input type="text" id="isbn" name="isbn" value="<%= book.isbn || '' %>" placeholder="978-0-123456-78-9">
            </div>
            
            <div class="form-group">
                <label for="total_copies">Total Copies:</label>
                <input type="number" id="total_copies" name="total_copies" min="1" value="<%= book.total_copies || 1 %>" required>
            </div>
            
            <div class="form-group">
                <label for="available_copies">Available Copies:</label>
                <input type="number" id="available_copies" name="available_copies" min="0" value="<%= book.available_copies || 0 %>" required>
            </div>
            
            <div class="form-group">
                <label for="price">Price ($):</label>
                <input type="number" id="price" name="price" min="0" step="0.01" value="<%= book.price || 0.00 %>">
            </div>
            
            <button type="submit" class="btn btn-success">Update Book</button>
            <a href="/books" class="btn">Cancel</a>
        </form>
    </div>

    <%- include('../partials/footer.ejs') %>
    <script src="/js/main.js"></script>
</body>
</html>